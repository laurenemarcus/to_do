<h1>Add a task to <%= @list.name() %> </h1>

<form action="/tasks" method="post">
  <input id="list_id" name="list_id" type="hidden" value="<%= @list.id() %>">

  <label for="description">Description of the task:</label>
  <input id="description" name="description" type="text">

  <label for="due_date">Due date for the task:</label>
  <input id="due_date" name="due_date" type="text" placeholder="mm/dd/yy">

  <button type="submit">Add task</button>
</form>

<% if @list.tasks().any?() %>
  <h3>Here are all the tasks in this list with their due dates:</h3>

  <ol>
    <% @list.tasks().each() do |task| %>
      <li><%= task.description() %>, <%= task.due_date() %></li>
    <% end %>
  </ol>
<% else %>
  <p>There are no tasks yet!</p>
<% end %>

<a href="/">Return to Main Page</a>
